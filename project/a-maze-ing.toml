[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "a-maze-ing"
version = "0.1.0"
description = "A-maze-ing Lib"
readme = "README.md"
authors = [@ecarabal, @swester]
requires-python = ">=3.10"
dependencies = [
    # Add standard team dependencies here, for example:
    # "numpy>=1.21.0",
]

[tool.setuptools.packages.find]
where = ["src"]